<!DOCTYPE html>
<html>
<head>
    <title></title>
    <meta charset="utf-8" />
</head>
<body>
     
    <h1>Departments</h1>
    <a href="/department/add">Add Department</a>
    
    <div>
        <% for(var i=0; i < department.length; i++) {%>
            <div>
                <p class="name"><%= department[i].department_name %></p>
                <a href="/department/edit/<%= department[i].id %>" class="">Edit Department</a>
                <a href="/personal/add/<%= department[i].id %>" class="">Add Personal<a>
                <a href="#" class="">All Personal<a>
                <form action="/department/delete/<%= department[i].id %>" method="POST" style="display:inline;">
                    <input type="submit" value="Delete Department" />
                </form>
            </div>
        <%} %>
    </ul>
</body>
<html>